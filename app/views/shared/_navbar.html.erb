<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container">
    <%= link_to "Articulo", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <div class="nav-item">
            <%= link_to "Descripcion", descriptions_path, class:"nav-link active", ariaCurrent:"page" %>
        </div>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">

        <% if customer_signed_in? %>
        <li class="nav-item">
            <%#= link_to 'Tus reacciones', my_actions_path, class: 'nav-link' %>
        </li>
        <% end %>

         <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false"> 
                <% if customer_signed_in? %>
                   Hola : <%= current_customer.email %>
                    
                <% else %>
                    Incia Sesión o Regístrate
                <% end %>
            </a>
            <ul class="dropdown-menu">
                <% if customer_signed_in? %>
                    <li><%= button_to 'Cerrar Sesión', destroy_customer_session_path, class:"dropdown-item", method: :delete %></li>
                <% else %>
                    <li><%= link_to 'Iniciar Sesión', new_customer_session_path, class:"dropdown-item" %></li>
                    <li><hr class="dropdown-divider"></li>
                        <li><%= link_to 'Registro', new_customer_registration_path, class:"dropdown-item" %></li>
                <% end %>
            </ul>
            </li>
        </ul>
    </div>
  </div>
</nav>